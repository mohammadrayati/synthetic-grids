.venv/
*.pyc
**/__pycache__/
.DS_Store
data-archive-download.tar.gz

# Real files (not tracked in git — too large, ~1.3G), fetched via `make download-data` from the
# shared Drive archive (see Makefile) - not tracked here since it's derived from the main
# project's own data/, not source.
data/